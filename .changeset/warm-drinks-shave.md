---
"@feneto/lh": patch
---

Fix `lh-discover` `agent: none` → `lh-scout` in OpenCode command bundle frontmatter, matching the `opencode.json` command config. Update Claude Code skill body text to remove contradictory "no scout subagent" language.
